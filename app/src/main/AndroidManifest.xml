<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.establishmentandroid">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.Camera.autofocus" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstablishmentAndroid">
        <activity android:name=".UpdateEmailPass" android:parentActivityName=".Profile_User" android:label="" android:theme="@style/forback"></activity>
        <activity android:name=".UpdateProfile" android:parentActivityName=".Profile_User" android:label="" android:theme="@style/forback" />
        <activity android:name=".Profile_User" />
        <activity android:name=".Unknown_qr" />
        <activity android:name=".Success_scan" />
        <activity android:name=".Signup" />
        <activity android:name=".Qr_Scanner" />
        <activity android:name=".Login_Form">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>